import React from 'react';
import Grid from "@mui/material/Grid";

const Database = () => {
    return (
        <Grid item xs={8}>
            <h3>Database</h3>
        </Grid>
    );
};

export default Database;